import * as THREE from "three";
import { MindARThree } from "mindar-image-three";
const startScreen=document.querySelector("#start-screen"),startButton=document.querySelector("#start-button"),stopButton=document.querySelector("#stop-button"),status=document.querySelector("#status");let mindarThree=null,pulse=null,sparkleGroup=null,clock=new THREE.Clock();
function makeSparkle(color=0xf1c9df){const g=new THREE.Group(),m=new THREE.MeshBasicMaterial({color,transparent:true,opacity:.85}),v=new THREE.Mesh(new THREE.PlaneGeometry(.035,.18),m),h=new THREE.Mesh(new THREE.PlaneGeometry(.18,.035),m);g.add(v,h);return g}
function buildProofEffect(){const g=new THREE.Group(),rm=new THREE.MeshBasicMaterial({color:0xe7b8d4,transparent:true,opacity:0,side:THREE.DoubleSide}),r=new THREE.Mesh(new THREE.RingGeometry(.075,.092,48),rm);r.position.set(0,.38,.02);pulse=r;g.add(r);sparkleGroup=new THREE.Group();[[-.22,.48],[.22,.48],[-.28,.22],[.28,.22]].forEach(([x,y],i)=>{const s=makeSparkle(i%2?0xe9d49b:0xdcb8e7);s.position.set(x,y,.025);s.scale.setScalar(.7+i*.08);sparkleGroup.add(s)});g.add(sparkleGroup);return g}
async function startAR(){startButton.disabled=true;startButton.textContent="Abriendo cámara…";mindarThree=new MindARThree({container:document.querySelector("#ar-container"),imageTargetSrc:"./targets/estampita.mind",maxTrack:1,uiLoading:"no",uiScanning:"no",uiError:"no"});const{renderer,scene,camera}=mindarThree;renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));renderer.outputColorSpace=THREE.SRGBColorSpace;const anchor=mindarThree.addAnchor(0);anchor.group.add(buildProofEffect());anchor.onTargetFound=()=>{status.textContent="¡La estampita cobró vida!";status.classList.remove("hidden")};anchor.onTargetLost=()=>{status.textContent="Apuntá la cámara a la estampita";status.classList.remove("hidden")};await mindarThree.start();startScreen.classList.add("hidden");stopButton.classList.remove("hidden");status.classList.remove("hidden");renderer.setAnimationLoop(()=>{const t=clock.getElapsedTime();if(pulse){const w=(Math.sin(t*3)+1)/2;pulse.material.opacity=.18+w*.55;pulse.scale.setScalar(.85+w*.35)}if(sparkleGroup)sparkleGroup.children.forEach((s,i)=>{const ph=t*2+i*1.4,a=(Math.sin(ph)+1)/2;s.material.opacity=.15+a*.8;s.rotation.z=Math.sin(ph)*.18});renderer.render(scene,camera)})}
function stopAR(){if(!mindarThree)return;mindarThree.stop();mindarThree.renderer.setAnimationLoop(null);stopButton.classList.add("hidden");status.classList.add("hidden");startScreen.classList.remove("hidden");startButton.disabled=false;startButton.textContent="Comenzar";mindarThree=null}
startButton.addEventListener("click",async()=>{try{await startAR()}catch(e){console.error(e);startButton.disabled=false;startButton.textContent="Intentar nuevamente";alert("No se pudo iniciar la cámara. Verificá que el navegador tenga permiso para usarla.")}});stopButton.addEventListener("click",stopAR);
